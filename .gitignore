# guards against the recursive src/src/ duplication this repo hit before
src/src/

# build output
/lpm
*.o
*.pic.o
*.a
*.so
*.so.*

# artifacts that have been accidentally committed before (see cleanup log) —
# keep these ignored so they can't silently reappear
/compiled
/log.txt
